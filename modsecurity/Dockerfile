FROM owasp/modsecurity-crs:nginx-alpine

# The base image already includes ModSecurity and OWASP CRS
# We just need to configure it for our use case

# Set working directory
WORKDIR /etc/nginx

# Expose port 80 (will be used internally)
EXPOSE 80
